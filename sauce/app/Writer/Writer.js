import React, { Component } from 'react';

class Writer extends Component {
  render() {
    return (
      <div>
        List of notes to choose from and select one to edit or publish/delete
      </div>
    );
  }
}

export default Writer;
